const fs = require("fs");
const fetch = require("node-fetch");

const API_URL =
  "https://yqtycmoqnkgdppjjukad.supabase.co/functions/v1/list-products";

function slugify(text) {
  return text
    ?.toLowerCase()
    .trim()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/(^-|-$)/g, "");
}

async function generateSitemap() {
  try {
    const response = await fetch(API_URL);
    const products = await response.json();

    const staticPages = `
    <url>
      <loc>https://bizharheritage.com/</loc>
      <changefreq>daily</changefreq>
      <priority>1.0</priority>
    </url>

    <url>
      <loc>https://bizharheritage.com/about</loc>
      <changefreq>monthly</changefreq>
      <priority>0.8</priority>
    </url>

    <url>
      <loc>https://bizharheritage.com/product</loc>
      <changefreq>daily</changefreq>
      <priority>0.9</priority>
    </url>

    <url>
      <loc>https://bizharheritage.com/craft-makers</loc>
      <changefreq>weekly</changefreq>
      <priority>0.8</priority>
    </url>

    <url>
      <loc>https://bizharheritage.com/contact</loc>
      <changefreq>monthly</changefreq>
      <priority>0.7</priority>
    </url>
    `;

    const productUrls = products
      .map((product) => {
        const slug = slugify(product.name);

        return `
        <url>
          <loc>
            https://bizharheritage.com/product/${product.id}/${slug}
          </loc>
          <lastmod>${new Date().toISOString().split("T")[0]}</lastmod>
          <changefreq>weekly</changefreq>
          <priority>0.9</priority>
        </url>
      `;
      })
      .join("");

    const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<urlset
 xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

${staticPages}

${productUrls}

</urlset>`;

    fs.writeFileSync("./public/sitemap.xml", sitemap);

    console.log("✅ Sitemap Generated Successfully");
  } catch (error) {
    console.error("❌ Sitemap Error:", error);
  }
}

generateSitemap();